# Q30: Write a Python program that will accept the base and height of a triangle and compute its area.

b = float(input("Enter the base(in cm): "))
h = float(input("Enter the height(in cm): "))

area = (1/2) * b * h

print(f"Area of the traingle is: {area} cm²")